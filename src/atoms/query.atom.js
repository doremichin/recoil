import {atom} from "recoil";

export const photosListState = atom({
    key : 'photosListState',
    default : {},
})

export const photosListByIdState = atom({
    key : 'photosListByIdState',
    default : {},
})
